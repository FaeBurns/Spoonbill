<Window x:Class="Spoonbill.Wpf.Frontend.View.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spoonbill.Wpf"
        xmlns:ext="clr-namespace:Spoonbill.Wpf.Frontend.Extensions"
        xmlns:viewmodels="clr-namespace:Spoonbill.Wpf.Frontend.Viewmodels"
        xmlns:userControls="clr-namespace:Spoonbill.Wpf.Frontend.View.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignInstance viewmodels:MainViewModel}"
        DataContext="{ext:DISource Type=viewmodels:MainViewModel}">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="0, 2"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <userControls:SelectPageTree Grid.Column="0" HorizontalAlignment="Stretch" DataContext="{Binding PageTreeHost}">

        </userControls:SelectPageTree>
    </Grid>
</Window>
